import React from "react";

//Develop purchase.js to allow collect user requested quantity
const purchase = () => {
  let title = "purchase page";

  return (
    <div>
      <h1>{title}</h1>
    </div>
  );
};

export default purchase;
